# Archivo de anulación para habilitar aceleración por GPU NVIDIA en el contenedor Ollama.
# Uso: docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d

services:
  ollama:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [ gpu ]
